{% extends 'base.html.twig' %}

{% block title %}{{ 'client.edit'|trans }}{% endblock %}

{% block body %}
    <div class="max-w-xl">
        <div class="page-header">
            <h1 class="page-title">{{ 'client.edit'|trans }}</h1>
            <a href="{{ path('app_client_index') }}" class="link">{{ 'common.back'|trans }}</a>
        </div>

        <div class="card">
            {{ include('client/_form.html.twig', {'button_label': 'common.update'|trans}) }}
        </div>

        <div class="mt-4">
            {{ include('client/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
