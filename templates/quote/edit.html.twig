{% extends 'base.html.twig' %}

{% block title %}Edit Quote{% endblock %}

{% block body %}
    <div class="max-w-xl">
        <div class="page-header">
            <h1 class="page-title">Edit Quote</h1>
            <a href="{{ path('app_quote_index') }}" class="link">Back to list</a>
        </div>

        <div class="card">
            {{ include('quote/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="mt-4">
            {{ include('quote/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
